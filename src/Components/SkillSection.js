import React from 'react'

const SkillSection = ({skill, img}) => {
    return (
        <div className="SkillSection">
                <div className="profile">
                    <p className="lang-image">{img}</p>
                </div>
        </div>
    )
}

export default SkillSection
